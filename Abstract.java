public class Abstract {
    public static void main(String[] args) {
        /* abstract = abstaract classes cannot be instantiated, but they can have a subclass
         *            abstaract methods are declared withiout an implementation
         */
        carrs car = new carrs();
        //vehicles vehicle = new vehicles();

        car.go();

    }
}
